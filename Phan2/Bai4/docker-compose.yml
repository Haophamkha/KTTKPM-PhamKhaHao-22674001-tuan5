services:
  app:
    build:
      context: ./app      
      dockerfile: Dockerfile
    container_name: node_app
    ports:
      - "3000:3000"
    volumes:
      - ./app:/app
      - /app/node_modules
    restart: always